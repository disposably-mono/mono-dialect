"use client";
// src/app/(game)/run/RunGameScreen.tsx
// STUB — replaced in Block C

export default function RunGameScreen() {
  return (
    <div style={{ padding: 40, color: "var(--text-muted)", fontFamily: "var(--font-mono)", fontSize: 13 }}>
      Game screen coming soon (Block C)
    </div>
  );
}
